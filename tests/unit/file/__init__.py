"""
Unit test package for file utilities.

This package contains test modules that validate the functionality of
the `genericlib.file` module and related file‑handling utilities.
It ensures correctness, reliability, and proper error handling across
different file operations.

Notes
-----
- All tests use `pytest` fixtures such as `tmp_path` for safe,
  isolated file creation.
- This `__init__.py` file marks the directory as a Python package,
  enabling discovery and organization of unit tests under `tests/unit/file`.

Usage
-----
Run pytest in the project root to execute these tests:
    $ pytest /work_dir/tests/unit/file
    or
    $ python -m pytest /work_dir/tests/unit/file
"""

